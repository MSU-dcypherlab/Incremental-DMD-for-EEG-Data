Unzip the downloaded the project to a specified folder.
Add the path of the specified folder to the MATLAB.
After run the codes in MATLAB, the results will will be saved in a folder named as "Results".
